import { TableContainerDirective } from './table-container.directive';

describe('TableContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new TableContainerDirective();
    expect(directive).toBeTruthy();
  });
});
